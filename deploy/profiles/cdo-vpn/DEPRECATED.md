# Deprecated

Use **`container`** with your hub host, port, and VPN notes in `.env`:

```bash
./scripts/configure-deploy.sh --type container --host YOUR_HOST --port 8080 --force
```

See [../README.md](../README.md).
