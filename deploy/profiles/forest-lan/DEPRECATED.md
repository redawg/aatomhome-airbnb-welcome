# Deprecated

Use **`container`** deploy type instead:

```bash
./scripts/configure-deploy.sh --type container --mode remote --deploy-host YOUR_HOST --port 8080 --force
./scripts/deploy-profile.sh container
```

See [../README.md](../README.md).
